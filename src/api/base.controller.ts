// Placeholder for cross-cutting controller concerns (auth/authorization, tenant
// scoping, etc). Kept as its own class - matching the reancare-service pattern -
// so those concerns can be layered in later without reshaping every controller.
export class BaseController {
}
